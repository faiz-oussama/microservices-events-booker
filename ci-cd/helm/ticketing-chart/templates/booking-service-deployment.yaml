apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: booking-service
    version: v1
spec:
  replicas: {{ .Values.bookingService.replicas }}
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
        version: v1
    spec:
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecret }}
      containers:
      - name: booking-service
        image: "{{ .Values.global.image.registry }}/{{ .Values.bookingService.image.repository }}:{{ .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.environment }}
        - name: SPRING_DATASOURCE_URL
          value: {{ .Values.bookingService.database.url }}
        - name: SPRING_DATASOURCE_USERNAME
          value: {{ .Values.bookingService.database.username }}
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.bookingService.database.passwordSecret }}
              key: {{ .Values.bookingService.database.passwordKey }}
        - name: SPRING_JPA_HIBERNATE_DDL_AUTO
          value: "update"
        - name: SPRING_JPA_SHOW_SQL
          value: "true"
        - name: SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT
          value: "org.hibernate.dialect.PostgreSQLDialect"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-service:8761/eureka/"
        resources:
          requests:
            memory: {{ .Values.bookingService.resources.requests.memory }}
            cpu: {{ .Values.bookingService.resources.requests.cpu }}
          limits:
            memory: {{ .Values.bookingService.resources.limits.memory }}
            cpu: {{ .Values.bookingService.resources.limits.cpu }}
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: booking-service
spec:
  type: {{ .Values.bookingService.service.type }}
  selector:
    app: booking-service
  ports:
  - name: http
    port: {{ .Values.bookingService.service.port }}
    targetPort: http
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: booking-service-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: booking-service
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}