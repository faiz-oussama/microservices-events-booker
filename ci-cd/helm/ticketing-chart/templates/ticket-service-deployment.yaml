apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticket-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: ticket-service
    version: v1
spec:
  replicas: {{ .Values.ticketService.replicas }}
  selector:
    matchLabels:
      app: ticket-service
  template:
    metadata:
      labels:
        app: ticket-service
        version: v1
    spec:
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecret }}
      containers:
      - name: ticket-service
        image: "{{ .Values.global.image.registry }}/{{ .Values.ticketService.image.repository }}:{{ .Values.global.image.tag }}"
        imagePullPolicy: {{ .Values.global.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.environment }}
        - name: SPRING_DATASOURCE_URL
          value: {{ .Values.ticketService.database.url }}
        - name: SPRING_DATASOURCE_USERNAME
          value: {{ .Values.ticketService.database.username }}
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.ticketService.database.passwordSecret }}
              key: {{ .Values.ticketService.database.passwordKey }}
        - name: SPRING_JPA_HIBERNATE_DDL_AUTO
          value: "update"
        - name: SPRING_JPA_SHOW_SQL
          value: "true"
        - name: SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT
          value: "org.hibernate.dialect.PostgreSQLDialect"
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          value: "http://discovery-service:8761/eureka/"
        resources:
          requests:
            memory: {{ .Values.ticketService.resources.requests.memory }}
            cpu: {{ .Values.ticketService.resources.requests.cpu }}
          limits:
            memory: {{ .Values.ticketService.resources.limits.memory }}
            cpu: {{ .Values.ticketService.resources.limits.cpu }}
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: ticket-service
spec:
  type: {{ .Values.ticketService.service.type }}
  selector:
    app: ticket-service
  ports:
  - name: http
    port: {{ .Values.ticketService.service.port }}
    targetPort: http
    protocol: TCP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ticket-service-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ticket-service
  minReplicas: {{ .Values.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.targetCPUUtilizationPercentage }}
